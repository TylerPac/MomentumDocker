services:
  momentum-app:
    build: .
    image: momentum-app  # <-- Added for tagging and rollback
    ports:
      - "8082:8080"  # <-- Changed to 8082 to avoid conflict with TylerPacDevelopment backend
    #user: root  # Ensures the app runs as root, avoiding permissions issues
    #volumes:
    #  - ./logs:/usr/local/tomcat/logs
    restart: always   # <-- ADD THIS
    environment:
      - MYSQL_HOST=shared_mysql
      - MYSQL_DATABASE=SirPacsterHomeLabDatabase
      - MYSQL_USER=sirpacster
      - MYSQL_PASSWORD=Kora4386!
    networks:
      - tylerpacdev_backend

networks:
  tylerpacdev_backend:
    external: true

